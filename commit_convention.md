# Angular Commit Convention 간단 요약


## 1. 기본 형식

```md
type(scope): subject
```

가장 많이 쓰는 형태는 아래와 같습니다.

```md
feat: 로그인 버튼 추가
fix: 회원가입 오류 수정
docs: README 업데이트
```

### 각 부분의 의미

- `type`: 커밋의 종류
- `scope`: 어느 영역을 수정했는지 선택적으로 표시
- `subject`: 무엇을 했는지 짧고 명확하게 작성

## 2. 가장 많이 쓰는 타입

| 타입 | 의미 | 예시 |
| --- | --- | --- |
| `feat` | 새로운 기능 추가 | `feat: 검색 기능 추가` |
| `fix` | 버그 수정 | `fix: 로그인 실패 오류 수정` |
| `docs` | 문서 수정 | `docs: 설치 방법 문서 정리` |
| `style` | 코드 의미 변경 없는 스타일 수정 | `style: 들여쓰기 정리` |
| `refactor` | 기능 변화 없는 코드 개선 | `refactor: 회원 서비스 로직 분리` |
| `test` | 테스트 코드 추가/수정 | `test: 로그인 테스트 추가` |
| `chore` | 기타 작업, 설정, 패키지 관리 | `chore: eslint 설정 변경` |
| `perf` | 성능 개선 | `perf: 목록 렌더링 최적화` |
| `build` | 빌드 관련 수정 | `build: vite 설정 변경` |
| `ci` | CI/CD 설정 수정 | `ci: github actions 배포 스크립트 수정` |

## 3. 자주 쓰는 형태

### scope 없이

```md
feat: 다크모드 추가
fix: 결제 오류 수정
docs: API 사용법 정리
```

### scope 포함

```md
feat(auth): 소셜 로그인 추가
fix(cart): 수량 계산 오류 수정
docs(readme): 실행 방법 업데이트
```

## 4. 초보자용 빠른 기준

- 새 기능을 만들었으면 `feat`
- 버그를 고쳤으면 `fix`
- 문서를 바꿨으면 `docs`
- 코드 정리만 했고 기능 변화가 없으면 `refactor`
- 설정 파일이나 패키지 버전 수정은 `chore`
- 테스트 관련 작업은 `test`

## 5. 좋은 커밋 메시지 예시

```md
feat: 회원가입 기능 추가
fix: 비밀번호 검증 오류 수정
docs: 프로젝트 실행 방법 추가
refactor: 사용자 조회 로직 분리
test: 로그인 API 테스트 추가
chore: prettier 설정 적용
```

## 6. 이런 식으로 쓰면 읽기 쉽다

- 짧고 바로 이해되게 작성하기
- 한 커밋에는 한 가지 주제만 담기
- `무엇을 했다` 중심으로 쓰기
- 너무 긴 설명은 커밋 제목 대신 본문에 작성하기

## 7. 가장 쉬운 시작 방법

처음에는 아래 3개만 잘 써도 충분합니다.

- `feat`: 기능 추가
- `fix`: 버그 수정
- `docs`: 문서 수정

예를 들면:

```md
feat: 상품 목록 페이지 추가
fix: 장바구니 합계 계산 오류 수정
docs: 환경 변수 설명 추가
```

---

실무에서는 여러 타입이 더 있지만, 초보자는 먼저 `feat`, `fix`, `docs`, `refactor`, `test`, `chore` 정도만 익혀도 충분합니다.
